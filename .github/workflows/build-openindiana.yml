name: Build (OpenIndiana)

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build on OpenIndiana
        uses: vmactions/openindiana-vm@v1
        with:
          usesh: true
          prepare: |
            pkg install -q --accept developer/versioning/git developer/gcc-14 developer/build/cmake developer/build/gnu-make developer/build/pkg-config x11/library/mesa

          run: |
            git clone --depth 1 --branch release-3.2.10 https://github.com/libsdl-org/SDL.git SDL3-src
            cmake -S SDL3-src -B SDL3-build -G "Unix Makefiles" \
              -DCMAKE_BUILD_TYPE=Release \
              -DCMAKE_INSTALL_PREFIX=/usr/local \
              -DSDL_SHARED=ON \
              -DSDL_STATIC=OFF \
              -DSDL_TEST=OFF \
              -DSDL_KMSDRM=OFF
            cmake --build SDL3-build
            cmake --install SDL3-build

            export PKG_CONFIG_PATH=/usr/local/lib/pkgconfig

            NCPU=$(nproc 2>/dev/null || getconf _NPROCESSORS_ONLN 2>/dev/null || echo 4)
            gmake -j$NCPU
            gmake server -j$NCPU
            # Fail loudly if a renderer module silently did not build. ref_vk is
            # required here because this platform ships Vulkan headers; platforms
            # without them skip it by design.
            for m in gl1 gl3; do
              test -f "build/release/ref_$m.so" || { echo "::error::ref_$m.so was not built"; exit 1; }
            done

            mkdir -p dist dist-ded
            cp build/release/heretic2r dist/
            # Renderer modules must sit next to the executable (the engine scans its own dir).
            cp build/release/ref_*.so dist/
            cp -r build/release/base dist/
            cp /usr/local/lib/libSDL3.so.0 dist/
            cp SDL3-src/LICENSE.txt dist/LICENSE.SDL3.txt
            cp build/release/heretic2r-server dist-ded/
            cp -r build/release/base dist-ded/
            tar czf heretic2r-openindiana-$(uname -m).tar.gz -C dist .
            tar czf heretic2r-server-openindiana-$(uname -m).tar.gz -C dist-ded .

      - name: Upload client artifact
        uses: actions/upload-artifact@v4
        with:
          name: heretic2r-openindiana
          path: heretic2r-openindiana-*.tar.gz

      - name: Upload dedicated server artifact
        uses: actions/upload-artifact@v4
        with:
          name: heretic2r-server-openindiana
          path: heretic2r-server-openindiana-*.tar.gz
